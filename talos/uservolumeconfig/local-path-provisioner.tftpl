${yamlencode({
apiVersion = "v1alpha1"
kind = "UserVolumeConfig"
name = "local-path-provisioner"
provisioning = {
  diskSelector = {
    match = "${match}"
  }

  minSize = "5GB"
  maxSize = "10GB"
}}
)}
